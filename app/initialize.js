import './styles/application.css';

//INFO - this import statement would trigger the App.js to execute and create the App object to start the application there.
import { App } from './components/App';

document.addEventListener('DOMContentLoaded', () => {

    //TODO - Has to start the app within App.js
    //TODO - Research how to export the App constructor so it can be started here instead.
    //const MyApp = new App(); // Instance of our Application
    //App.start()
    console.log("app is started");

});
